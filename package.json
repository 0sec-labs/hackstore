{
  "name": "hackstore-registry",
  "private": true,
  "scripts": {
    "build": "node scripts/build-index.mjs",
    "check": "node scripts/build-index.mjs --check",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=22" },
  "devDependencies": { "ajv": "^8.17.1" }
}
